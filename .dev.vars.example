# 本地开发机密示例（架构 §15.5）
#
# 用法：复制为 `.dev.vars`（已被 .gitignore 忽略）并填入真实值：
#   cp .dev.vars.example .dev.vars
# 生产环境不放这里：在 Cloudflare 部署页的机密项里填写，
# wrangler.jsonc 的 "secrets": { "required": [...] } 已声明需要哪些机密。

# AUTH_PEPPER —— 登录校验与假盐派生用（架构 §13.2、需求 §5.4）
#   用途：auth_verifier = HMAC-SHA256(AUTH_PEPPER, 浏览器派生的登录密钥)；
#        prelogin 对不存在的用户名返回 HMAC-SHA256(AUTH_PEPPER, "menote-prelogin-v1:<用户名小写>") 的前 16 字节。
#   生成：openssl rand -base64 32
AUTH_PEPPER=<openssl rand -base64 32 的输出>

# 后续里程碑新增的机密在此追加（并同步 wrangler.jsonc 的 secrets.required）：
# BACKUP_CRED_KEY —— M5：备份内容密钥 K 的包裹键
