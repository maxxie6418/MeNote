# AGENTS.md
本文件指导本仓库的 AI 编程代理。只放高频、必须遵守的规则。规则并非一成不变，见「规则可演进」。产品、架构、API、数据、部署、安全、测试的细节以 `wiki/` 中的定稿为准；定稿没有的，再看 `docs/`（不要读 `docs/archive/`，除非任务明确指定）。

开始任务：先读本文件（含下方项目简介），再按任务读对应目录里的文档和相关代码。涉及界面时同时读根目录 `DESIGN.md`。

## 项目简介

- 名称：`MeNote`
- 一句话：面向个人与家人的轻量多端笔记应用（浏览器 PWA + Cloudflare 自托管），支持 Markdown 笔记、表格、Memo、待办、版本历史与隐私锁。
- 仓库形态：单仓多包（规划 pnpm workspace：`apps/web` + `apps/worker` + `packages/*`，见架构文档 §2.3）
- 当前版本：预开发（尚无代码，仅设计文档与原型）
- 技术栈：
  - 前端：`React PWA（Vite + CodeMirror 6，规划）`
  - 后端：`Cloudflare Workers（Hono，规划）`
  - 数据：`Cloudflare D1 + R2 + Static Assets；外部备份 WebDAV / S3 / Git`
  - 部署：`GitHub 一键部署到 Cloudflare（Workers Builds，见架构文档 §15.5）`
- 源码位置：代码尚未初始化；规划 `apps/web`（PWA 客户端）与 `apps/worker`（Cloudflare Worker）
- 界面样板：`prototype/menote-prototype.html`（高保真交互原型）、`prototype/menote-framework.html`（线框评审页）；`DESIGN.md` 待从原型设计令牌落稿
- 怎么跑：尚未初始化；初始化后更新此处（完整说明见 README.md）
- 现在做到哪：设计与架构定稿阶段（需求 v7.4 / 功能拆解 v2.3 / 架构 v1.5），下一步初始化代码骨架

## 工作原则

- 先读现有实现，再改；范围保持最小。优先复用已有组件、函数、服务、类型和配置。
- 不引入与任务无关的重构，不绕过现有架构边界。
- 新增代码和文档放在职责明确的位置。不要随意新建顶层目录，或把临时逻辑堆在不相关文件里。文件夹里的具体文件不预先规定，按需要创建即可。
- UI 只做展示和交互。复杂逻辑下沉到业务模块、服务层或工具函数。数据访问、外部 API、配置读取集中封装，不要散落在页面、组件或路由中。
- 客户端代码不得读取服务端密钥。样本数据只用小型、脱敏、可公开的示例。
- 需求与现有实现或已生效文档冲突时，停下来指出冲突点，等人确认。有更简单、更安全、或更符合现有架构的方案时，主动说。
- **规则可演进**：本文件的限制并非一成不变。项目推进中若有更合适的约定，主动提出（现状问题、建议改法、影响范围），用户确认后再改本文件；未确认不改。随项目进行需要注意、尚未升入上方稳定章节的内容，写入文末「额外补充」。
- **反复失败则停**：同一问题反复尝试仍错，停下来告知用户（已试过什么、仍错在哪），确认接下来的方法路线是否可行。一般不超过三次。
- 没验证过，不要声称任务已完成。
- 新增或改动代码时，先查 `wiki/Menote-项目架构-v1.md` §2.3.2 的「功能→代码落点对照表」再落位：后端按业务域归入 `apps/worker/src/routes/` 与 `services/` 的对应文件（如 routes/items.ts、routes/sync.ts、routes/attachments.ts），用 Hono 子路由 import 接入；前端按 feature 目录（ui/ + model.ts 两件套）归位。本项目是 Cloudflare Workers，**没有 server.mjs**——唯一入口 `apps/worker/src/index.ts` 只做装配（挂子路由、导出 fetch/scheduled），禁止把业务逻辑写进任何入口文件；文件行数预算与依赖方向约束见架构文档 §2.3.1 / §2.3.3。




## 先确认再动

除非任务明确要求，不要：大范围重构、换技术栈、加生产依赖、改运行时/包管理器/部署方式/端口/环境变量约定。

下列改动先说明影响范围，等人点头：

- 数据库 schema、迁移、字段含义、数据删除、存储路径
- 认证、权限、安全策略
- API 返回结构和错误格式
- 部署、环境变量、端口、运行时版本
- 公共组件、公共类型、公共工具、全局配置
- 全局视觉（主题、字体、组件库、导航结构）
- 根目录 `DESIGN.md`（改、删、覆盖都算）
- `wiki/` 中的任何已有文件（改、删、覆盖都算）

## 文档放哪

只约定文件夹和根目录这几个文件。各文件夹内的文件由 AI 按任务创建，不预置文件名。

```text
项目根目录/
├─ README.md
├─ AGENTS.md
├─ DESIGN.md             # 界面与交互。视觉源
├─ CHANGELOG.md
├─ wiki/                 # 定稿。禁止擅自改、删
└─ docs/
   ├─ todo/              # 专项计划。一个功能/任务一份实施计划
   ├─ modules/           # 各专项功能的设计文档
   ├─ scratch/           # 草稿。临时讨论，一份讨论一份文件
   └─ archive/           # 归档。默认不读
```

- `README.md`：介绍和怎么跑起来。
- `DESIGN.md`：界面与交互规范（视觉语言、组件用法、布局与密度、禁止项）。有界面的项目作为**唯一视觉源**。尚未创建时不要擅自写一份塞进仓库，先问用户。没有界面的项目可不建。冲突时：产品对错看 `wiki/`，长什么样看 `DESIGN.md`，某一屏的结构看 `docs/modules/`。
- `wiki/`：已定稿、作为当前依据的文档。不能随意修改或删除。写入新定稿、修订或删除已有文件，都必须先得到用户明确同意。
- `docs/`：尚未定稿的文档和过程资料。冲突时以 `wiki/` 为准。
- `docs/modules/`：各专项功能的设计文档。一个功能一份（或一摊）设计，按需要创建，不预置文件名。
- `docs/todo/`：专项计划。一个功能或任务一份实施计划（目标、拆步、涉及文件、验收点），开工前创建；做完整份移入 `docs/archive/`。
- `docs/scratch/`：草稿区。临时讨论与方案草稿，一份讨论一份文件（主题命名，可带日期），随手写、不设格式门槛；内容定稿后搬往 `todo/`、`modules/` 或 `wiki/`，过时件定期清理，不作为实现依据。
- `docs/archive/`：过期、废弃、被替代的文档。默认不读取、不检索、不作为实现依据，除非用户或任务明确指定其中的文件。
- 测试素材、样本数据放 `docs/` 下自建目录，并加入 `.gitignore`，禁止提交。
- 源码、定稿、过程文档严格分区。新文件先归入已有文件夹。

### 文档维护

`docs/` 里需要长期保留的文档，头部写：`文档版本`、`文档状态`（草案 / 评审中 / 生效 / 废弃 / 归档）、`目的和适用范围`、`权威级别`（全局规则 / 模块规则 / 临时规则 / 历史参考）、`最后更新日期`（`YYYY-MM-DD`），以及修改记录表（文档版本号、应用版本号、日期、修改摘要、修改模型ID）。文档版本号与应用版本号分开。

`docs/` 以追加为主。要大改：旧文档标废弃，移入 `docs/archive/`，再另建新文件，不要覆盖原文。不要直接改 `wiki/` 来迁就新需求；定稿后搬进 `wiki/`，须经用户同意。

时效性文档文件名可带日期或版本、主题，便于检索。

## 新功能怎么做

1. 先看 `wiki/` 定稿，再看 `docs/modules/` 里对应专项和本次需求。不要翻 `docs/archive/`。
2. 用几句话向用户简述思路：做什么、不做什么、关键取舍、准备动哪。功能没讲透、方向没被确认之前，不写代码、不改仓库、不补大段设计。
3. 用户确认方向后再推进。相关设计还没有、且功能能闭环，先在 `docs/modules/` 补该专项的设计文档，不要直接改 `wiki/`。
4. 设计 → 功能点 → 技术方案和实现计划 → 再写代码。实施计划落 `docs/todo/`，设计落 `docs/modules/`。涉及新屏或大改布局时，先走「界面怎么做」，用户确认稿后再写界面代码。
5. 中途需求和定稿或已生效文档冲突：停，声明，等人确认。

新功能没有「先做再说」。只有范围已说清、不是新功能、且不碰高风险区的小改动，可以直接做。碰到上一节列出的区域，先说影响和建议。

## 界面怎么做

没有界面的项目跳过本节。

- 界面设计遵循less is more原则
- **视觉源只有一份**：根目录 `DESIGN.md`。颜色、字号、间距、圆角、组件用法从那里抄；现有页面只作补充，不得另立规范。禁止另引一套 UI 库，禁止为单个功能另起视觉语言或另做一套页面骨架。
- **先稿后码**：新屏或明显改布局，先读 `DESIGN.md`，再在 `docs/modules/` 写清这一屏有哪些块、主操作、空状态（可附图或原型）。用户确认前不写界面代码、不改全局样式、不改 `DESIGN.md`。
- **本程只做本程的屏**。不要顺手改无关页面、主题或导航。
- **实现只对照 `DESIGN.md`、本屏稿和已有组件**。缺的组件用 `DESIGN.md` 指定的体系拼，不手写第二套。
- 改完对照 `DESIGN.md` 与本屏稿说明（或附图）。没有对照不算做完。
- **界面辅助文案默认不写**。确需的说明性文字（用途、口径、背景）一律收进 `InfoHint`
  （ⓘ + 悬停，），不得平铺；警告、破坏性后果、错误/校验、实时计数**必须保持可见**。





## 做完怎么交

说明：修改摘要、跑过的验证命令和结果、未验证内容或剩余风险。没法跑验证就写原因，不要假装验证过。

每次改动必须在根目录 `CHANGELOG.md` **最上方**追加一条无序列表，一句话说明改了什么，并带上应用版本号；若本次提交了 git，同一条里写上 commit hash。**日期用小标题**（`## YYYY-MM-DD`）：同一天的条目都写在同一个日期标题下，不重复写日期；当天的小标题不存在时在文件顶部新建。格式：

```markdown
## YYYY-MM-DD

- vX.Y.Z / <commit-hash> — 一句话
- vX.Y.Z — 一句话（无 commit 时省略 hash）
```

### 版本号规范【已定·用户确认 2026-09-26】

- 格式 `v主.次.修`；根 `package.json` 的 `version` 字段为唯一当前版本，CHANGELOG 条目与之一致。
- **主版本号**：仅用户主动提出才变更（如转正发布 1.0.0），任何人不得自行升位。
- **次版本号**：跟随里程碑——每个里程碑完成收口时 +0.1（M0 → v0.1.x，M1 → v0.2.x，……，M6 → v0.7.x）。里程碑开发期内的改动只动修订号，收口时一次性 +0.1。
- **修订号**：优化、修复、小功能调整每项 +0.0.1；同一问题的分批调整可合并，全部完成后一次性 +0.0.1。
- 纯文档 / 约定类改动不升版本号，CHANGELOG 条目沿用当前版本号。

Git：

- 代码和文档分开提交。
- commit 用中文 Conventional Commits。
- 不提交密钥、`.env`、本地数据库、上传文件、测试素材、构建缓存。
- 身份匿名化（强制）：只用 GitHub noreply 邮箱。禁止在提交信息、分支名、Tag、文件内容里写真实姓名、私人邮箱、手机号等个人身份信息。
- Git 操作后返回最近提交或状态，方便确认。


## 额外补充

随项目进行需要注意的内容记在这里：工作习惯、易踩坑、临时约束，以及尚未升入上方稳定章节的约定。确认后写入；过时则标注。

- 