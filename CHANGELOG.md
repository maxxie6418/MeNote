# CHANGELOG

> 每次改动在最上方追加；日期用小标题，同一天条目不重复写日期。版本号规范见 AGENTS.md「版本号规范」：主版本号仅用户主动提出变更；次版本号随里程碑收口 +0.1；修复/优化/小功能 +0.0.1（同一问题分批调整可合并后一次 +0.0.1）；纯文档改动沿用当前版本号。

## 2026-09-26

- v0.2.11 — **AGENTS.md 再补一条提交纪律（用户要求）**：`git commit` 之后、`git push` 之前**先核对** `git log -1 --format="%s"` 与预期首行一致——消息文件可能因编辑工具的版本守卫而没写进去，此时 `-F` 会静默沿用它上一次的内容（本仓库已发生两次：一次把整轮功能代码混进 `docs:` 提交、一次把 v0.2.11 的文档提交标成了 v0.2.10 且已推送）。不一致且未推送就 `--amend`；已推送的不追改、不做 force-push。**说明**：`364fd03` 这个提交的**内容**是正确的（CHANGELOG v0.2.11 条目、数据模型稿 v1.3、M2 计划 M2-5 完成），只有提交信息写成了 v0.2.10/M2-4
- v0.2.11 / 20ccb63 — **M2-5 待办：独立视图 + 看板 + 任务字段约束**。worker 侧新增 **0002 迁移**：6 个幂等触发器定死 `task_status ∈ {todo,doing,done}`、`task_priority ∈ {high,medium,low}`（NULL 放行），并禁止 `is_task=0` 时携带任务字段，覆盖 INSERT 与 UPDATE；**用触发器而不是给 `items` 补 CHECK**，因为 SQLite 补 CHECK 只能重建整张热表（复制/删表改名/重建索引），风险与收益不成比例，触发器幂等追加、不动表结构（已纳入 selfheal 完整性检查）。web 侧 `features/tasks`：按状态分组与排序（有截止在前 → 优先级 → 最近更新）、状态/优先级/截止三档**纯本地**筛选、各状态实时计数；`TaskPanel`（列表 / 看板切换）、`TaskListView`、`TaskKanban`（三列固定顺序）、`TaskFilterBar`、`TaskCard`（列表与看板共用，改状态用文字按钮「开始 / 完成 / 重开」，不把拖拽当唯一入口）；改状态同时改 md 的 front matter 与派生列；去掉清单标记走 Q23（删 `task` 字段并清空三列）。录入框待办档接入**真实控件**（原生日期输入 + 优先级三段，默认"中"），新建清单默认状态"待办"，发布后保持待办模式；浏览三段的待办项由禁用转为可用。新增 31 个用例（worker 7 + 待办模型 17 + 视图 7）；全仓 **335** 个测试通过；首屏 125.5 KB
- v0.2.10 / 837225e — **M2-4 收尾：Memo 转笔记（Q10）**。`packages/mdcore` 的 front matter 新增 **`converted_to`** 键（可选：解析/写出/行级改写，传 `null` 删键，其它键与正文一字不动）——关联放 md 里而不是本地表，因为它必须跟着数据同步，否则换设备打开那条链接就没了。`features/memos/actions.ts` 的 `convertMemoToNote()`：标题取正文首行（**≤50 字**，按码点截断不切半汉字）、正文为其余内容、**清单字段不带走**、标签写进笔记 YAML、新笔记落根目录、原 Memo **保留**并只加关联键（避免时间线出现缺口）、已转过则幂等。界面：Memo 菜单加「转为笔记」（转过之后不再出现），转过显示「已转为笔记 · 打开」链接，转换后自动回到笔记视图并直接打开新笔记。10 个新用例；全仓 **301** 个测试通过；首屏 123.4 KB
- v0.2.9 — **AGENTS.md 固化 PowerShell 使用禁忌（用户要求）**：把这一路上踩过的四类事故写进「额外补充」并定成规则——① `Set-Content -Encoding UTF8` 写出 BOM；② 未显式声明编码把 UTF-8 当 ANSI 解码，两份中文文档被写坏（236 行变 132 行）；③ `Set-Content -NoNewline` 把 `package.json` 压成一行；④ 替换串含 ASCII 双引号导致解析报错。规定：源码/文档/配置一律用编辑工具改，写 JSON 用 `node -e` + `JSON.stringify(obj, null, 2)`，shell 只做只读探查、构建、测试、git。同时新增「提交信息不要塞进命令行」一条（here-string 里的 ASCII 引号会截断消息、让改动留在暂存区被随后的提交带走，改用 `git commit -F 文件`，暂存点名文件而非整目录）。并把项目简介的版本与「现在做到哪」对齐到当前进度
- v0.2.9 — **M2-4 Memo：发布与时间轴（可用了）**。录入框 Memo 档接入**乐观发布**（先落本地并标"待上传"，由 outbox 后台上传；写入永远免密），正文写了 `- [ ]` 时在附加项位置提示**「设为清单？」**，点一下才带清单标记（不自动改语义），把清单项删掉标记自动作废；新增 `MemoPanel` / `MemoTimeline` / `MemoItem`——按天分组的**时间轴**（天边界按设置时区）、渲染后的正文（`MarkdownPreview`，数据层已剥 front matter）、标签、时间、清单小图标、置顶标记；顶部**标签筛选 + 日期范围筛选**（今天/近 7 天/近 30 天/全部）与**「添加」按钮**（把焦点送回录入框）；**原位编辑**（Q19：`Ctrl+Enter` 保存、`Esc` 取消，`memo_at` 不变）；**不提供收藏**（Q8）。浏览三段的 Memo 项由禁用转为可用，顶栏面包屑随视图变化；front matter 由数据层按"是否清单 + 新标签"重新生成，用户只编辑内容，看不到也改不坏 YAML。新增 11 个用例（时间轴分组与渲染、标签/范围筛选、清单图标、无收藏入口、原位编辑的保存与取消、置顶、录入框的清单提示与乐观发布）。首屏 118.7 → **122.7 KB**
- v0.2.8 — **修线上登录故障（客户端兜底 http → https）**：用户用 `http://me.861306.xyz` 打开时一直提示"浏览器不提供加密能力"，诊断串定位到根因——`协议 http: · 安全上下文 否 · WebCrypto 缺失`（http 下浏览器不提供 WebCrypto，而 Cloudflare 的「Always Use HTTPS」默认关闭，直接敲域名就落在 http 上）。新增 `httpsUpgradeUrl()` 并在 `main.tsx` 挂载前 `location.replace` 升级：http 且主机像公网域名才跳（保留路径/查询串/hash）；`localhost`/`127.0.0.1`/`*.local`/纯 IP/单标签主机名一律不跳（那些环境没有 TLS，且 localhost 下 http 本就是安全上下文）。**治本手段仍是云端开 Always Use HTTPS**（已告知），这道兜底让"忘了开开关"或"用 http 链接打开"不会把用户堵死。4 个新用例覆盖升级与不升级的边界
- v0.2.8 — **M2-4 前置：Memo 数据层与时间轴模型**：`listLocalItems` **排除 Memo**（Q8：笔记/收藏/标签视图都不列 Memo——此前是个潜在漏洞，因为还没人建过 Memo），新增 `listLocalMemos`；`createLocalItem` 支持清单字段（`is_task` / `task_status` / `task_due` / `task_priority`）；`features/memos/model.ts` 落**按设置时区**（默认 `Asia/Shanghai`）分天的时间轴分组、置顶在前（Q9）、标签与日期范围筛选、标签云统计（13 个用例，含"北京时间 00:30 属于当天而 UTC 还在前一天"这类跨时区边界）。UI 接线（发布 Memo、时间轴面板）在下一步
- v0.2.7 — **M2-3 收尾：文件夹重命名与移动（含一个真 bug 修复）**：新增共享控件 `Modal`/`Overlay`（点遮罩与 `Esc` 关闭、打开时焦点移入、`role="dialog"`），字段与列表样式按 §4.3 刻度收敛；笔记本树每个节点加「更多」菜单——**重命名**（弹窗改名，空名禁用保存并说明原因）与**移动到…**（弹窗列出候选目标，**非法目标置灰并写明原因**：已在根目录 / 已经在目标里 / 不能移到自己的子文件夹 / 移进去会超过两层 / 下面还有子文件夹）；第 2 层节点**不出现**"新建子文件夹"入口（该位置没有合法动作）。规则抽成 `features/notes/folders.ts` 的纯函数 `folderMoveTargets`，与服务端 `depthUnder` 口径一致。**修掉一个真 bug**：`+` 菜单原先固定往根目录建，忽略了当前选中的文件夹 —— 于是"选中第 2 层时新夹建在父层"这条规则实际没生效（正是验收点要求的行为）；改为按选中项决定父级。新增 11 个用例（两层规则与移动目标 9 项、面板的重命名/移动/入口收敛）。首屏 117.5 → **118.7 KB**
- v0.2.6 — **M2-3 笔记本与文件夹（客户端闭环）**：服务端的创建/改名/移动与两层校验 M1 已完成，本轮补齐客户端——`data/db` 新增文件夹本地写（`createLocalFolder` / `renameLocalFolder` / `moveLocalFolder` / `enqueueFolderPatch`，合并规则与条目元数据补丁一致）、文件夹计数与**行内摘要**（取已缓存正文第一行，不加派生列）；`features/notes/ui` 新增 `NotebookPanel`（组头 + `+` 菜单 + **内联命名**：Enter 确认 / Esc 取消 / 空名不创建）、`FolderTree`（两层、节点计数、待上传标记）、`NbAddButton`（新建文件夹可用、**新建表格禁用说明 M5 提供**）；条目列表行补齐**置顶/收藏标记与摘要**，并加行内「更多」菜单（移动到根目录/各文件夹、置顶、收藏）；`views.ts` 新增两层限制的纯函数（`folderDepthFor` / `canCreateChildFolder`）与文件夹视图筛选。结构上把笔记本分组整块移到 features 并由 App 作为插槽交给功能栏——`app/` 不反向依赖 feature。视图计数、视图筛选、两层限制与面板交互共 **10 个新用例**。首屏 116.0 → **117.5 KB**
- v0.2.6 — **修正 M2-3 的一处计划内部矛盾**：验收点原写"删除文件夹（Q12）含确认框"，但同一份计划的**范围边界**明确把回收站界面划给 M4/M12，且 outbox 没有删除类 op（M1 已定删除走 M4 的 trash 通道）。**删除/恢复整体后移到 M4**，与回收站一起做（没有回收站的删除对用户不可逆，属"能不做就不做"）；Q12 的规则（确认框写清数量、保留原路径、恢复时父不在则回根目录）继续留在 Q 表里作为 M4 的验收依据。M2-3 剩余项：文件夹重命名/移动的界面入口（需要 Modal 或行内编辑，下一轮）
- v0.2.5 — **加密能力提示改为自证式诊断（修一个真实的误判）**：用户用 https 访问却看到"当前页面不是安全连接（http）"——那条提示把原因写死了，而浏览器实际状态可能是"证书警告被跳过""页面被嵌在 http 的 iframe 里""经由代理/内置浏览器打开""浏览器过旧没有 WebCrypto"。现在 `cryptoEnvironment.ts` 探测并**显示事实**（地址 / 协议 / 安全上下文 / WebCrypto），横幅与登录报错都带上这一行，用户可原样反馈；证书本身已实测有效（Google Trust Services，SAN 含 `me.861306.xyz`，Node 严格校验通过），所以不是证书问题。新增 6 个用例（含"https 下被判非安全时不得臆断成 http"）
- v0.2.5 — **M2-2 界面框架填充**：抽出共享控件 `SegmentedControl`（盒式分段控件，录入框模式行与正文区模式切换共用）、`NavItem`、`Chip`（实底/紧凑/标签三形态）；功能栏补齐结构——`NavSegmented`（浏览三段=下划线页签，与盒式刻意区分）、`NavList`（最近编辑/收藏）、`NotebookGroup`（笔记本+计数）、`TagGroup`（标签云来自 `items.tags`，可点筛选）、`VaultNode`（加密空间**贴底固定**、在滚动容器之外、M2 只做外观占位）；主操作区抽出 `TwoPane` / `ItemListHead` / `EmptyDocPanel`；`views.ts` 新增视图筛选与标签统计（纯函数）；录入框三行结构落地并接通**笔记档发布**（首行作标题，用 `@menote/mdcore` 的 `splitFirstLineAsTitle`），Memo/待办档禁用并说明原因。新增 17 个用例：DOM 结构不变量（加密空间是滚动区兄弟、功能栏内无账户区、两种导航造型可区分）、三档附加项（note 档**无加密胶囊**）、Ctrl+Enter 发布、视图筛选与标签统计。按"不做空入口"的既有口径，浏览三段中未接入的首页/Memo/待办**禁用并说明原因**（M2-8/M2-4/M2-5），`Ctrl/Cmd+K` 聚焦搜索留待 M2-6 与搜索框一起接（现在搜索框仍是禁用占位）。首屏 113.1 → **116.0 KB**（新增组件与 mdcore 入库），仍远低于 200 KB
- v0.2.4 — **M2 开工：新增 `packages/mdcore`（Markdown 核心，M2-1）**。零运行时依赖的纯函数包：`frontmatter.ts` 自实现**有界 YAML 子集**解析（`menote:` 根键、标量、内联/块序列、一层嵌套），并给出**行级安全改写**——只替换被 patch 命中的键，表格的 `columns`/`views` 这类不解析的键**原样保留**（否则保存一次就吃掉表格元数据）；格式损坏时降级为普通正文而不抛错（需求 §10.2）。`tags.ts` 派生标签（YAML `menote.tags` + 正文 `#标签`，剔除代码块与行内代码、`#` 前必须是行首或空白所以网址锚点不算、大小写不敏感去重）。`tasks.ts` 定死任务字段字面量：**写入 `todo/doing/done` 与 `high/medium/low`**（英文，与既有 YAML 键约定一致）、**读取兼容中文**、日期 `YYYY-MM-DD` 且校验真实日期、三个字段都可为空；`is_task` 的判据是「有没有 `menote.task` 键」（Q23 去掉清单标记即删字段）。`markdown.ts` 提供首行一级标题取值与录入框「首行作标题」。**35 个用例**；接入根 tsconfig 引用与 dev/build/test 链路。同时把数据模型稿 §六待确认 #1（任务枚举字面量）由「待定」记为「已定」
- v0.2.3 / d4601d0 — **视觉定型：配色整体换成 Cloudflare 色系**（用户确认「按提案定稿」）。① 原型两个页面（`menote-prototype.html`、`menote-framework.html`）令牌块换色、14 处硬编码渐变全部改为令牌，回归脚本实测 **55/55 与 35/35**；② 应用 `tokens.css` 换新令牌并去掉「临时值」标注，`app.css` 圆角收敛到 3 档（0 处裸圆角、0 处拿间距令牌当圆角），橙色文字改走新令牌 `--primary-ink`；③ 新增配色守卫（浅深令牌成对、主色与深墨、旧暖色不得残留），已验证有牙；④ **顺带修好一个根本跑不起来的工具**：两个原型回归脚本是 CommonJS 而仓库是 `type: module`，此前执行只会报 `require is not defined`，用 `prototype/package.json` 的目录级 `type: commonjs` 修复。三条由对比度实测逼出的硬结论：**橙底必须深墨**（`#1D1D1D` on `#F6821F` = 6.53:1；白字仅 2.58:1）、**橙色不能当浅色文字色**（新增 `--primary-ink #B45309` = 5.02:1）、**警告色与主色同属暖色**故状态必须带图标或文字。落地顺序与全部对比度见新增的 `docs/modules/Menote-视觉定型提案-v1.md`
- v0.2.3 — DESIGN.md 视觉章定稿（文档 v1.2）：§3 由【待定】改【已定】并写入令牌表（配色 / 字族 / 字号 6 档 / 圆角 3 档 / 阴影 3 档）；§3.4 四个连带问题标记已解决；§4 间距刻度定稿（4px 基数 6 档 + 三档密度映射）
- v0.2.2 / 33b58cd — 恢复被 PowerShell 损坏的两个文件：上一提交用 `Get-Content -Raw` + `WriteAllText` 改交叉引用时，Windows PowerShell 默认按 ANSI 解码 UTF-8，导致 M1 归档计划（236 → 132 行、中文成乱码）与 CHANGELOG 被损坏；已 `git checkout` 回滚并用编辑工具重做。**教训（第三次踩同一根源，已列入 AGENTS.md 候选补充）：仓库文件只用编辑工具改，不要走 shell 重定向 / `Set-Content` / `Get-Content -Raw` + `WriteAllText`**
- v0.2.2 / 4cffa2c — 修正 M2 计划小节编号（原 1.1 → 1.3 → 1.4 → 1.2 重复错序，改为 1.1 文档 / 1.2 M1 遗留清单 / 1.3 待拍板 Q 项）并同步交叉引用
- v0.2.2 — 修掉版本一致性守卫自身的 CRLF bug：仓库存 LF、Windows 检出后工作区可能是 CRLF，`split("\n")` 每行尾部带 `\r` 会让日期小标题正则匹配失败（本地红、Linux CI 绿）。改为读取时归一化换行——这个守卫本身就是为了防"本地与 CI 结论不一致"，自己先栽在这上面很讽刺
- v0.2.2 — M1 安全审查 + 多用户隔离回归测试：对 M1 的 Worker/前端代码做了一次针对性审查（注入面、越权、机密泄漏、会话与 Cookie、限流、日志卫生），**未发现高危问题**；其中"知道对方条目 id 也动不了对方数据"这条最强越权路径此前没有用例，补 2 个测试钉死（同 id 跨用户创建 → 409 且**错误详情不回显对方 rev/哈希**、跨用户读正文/存正文/改元数据 → 404、A 的正文标题版本未被改动、B 的拉取看不到 A 的行）。审查确认的良好性质：无 SQL 字符串插值（唯一的动态 SQL 用编译期白名单列）、所有用户数据读写都带 `user_id`、Cookie 为 HttpOnly+SameSite=Lax+Secure(仅 https)+Path=/、会话有 TTL 与滑动续期且改密踢掉其他会话、机密不出现在客户端源码与产物里、只在三处 console.error 且不打令牌
- v0.2.2 — 版本号一致性守卫 + 门面文档订正：① 新增 4 个用例盯住 AGENTS.md 的版本规范（`package.json` 的 version 格式；CHANGELOG 最新日期小节里必须有当前版本条目；条目不得游离在日期小标题之外；**条目里的版本不得高于 package.json**）——已验证有牙（version 改 0.2.3 而不记 CHANGELOG → 报红并说明原因；改回即绿、无 BOM 残留）。② `README.md` 订正过期内容：状态从"M0 骨架已完成"改为"M1 已收口"，技术栈表删掉**从未采用**的 Tailwind CSS（实际是 DESIGN.md 令牌层 + 原生 CSS）并把 CodeMirror/Dexie/Valibot 标为已接入，`pnpm test` 说明补上 web 测试，部署章节补**部署后必须加 `AUTH_PEPPER` 机密**（含生成命令与"不可更换"的警告）与 Always Use HTTPS 建议，快速开始补"必须 localhost 或 https"与"删 `apps/web/.wrangler/state` 即重置本地数据"。③ 顺带审计：设计稿 §3.2 的登录失败节流**已完整实现**（5 次免罚、30s 起翻倍、15 分钟封顶、用户名与 IP 双键、成功后清零）且有 429 用例，无缺口
- v0.2.2 — 结构与视觉不变量的静态守卫：新增 7 个用例守住 DESIGN.md 标【实测】的硬约束（顶栏 54 / 功能栏 294 / 列表 330 / 设置导航 184 / 圆角 10·14；功能栏算式 54+12+38+12+136=252；录入框三行 40–180 可纵向 resize、附加项固定 26 且 nowrap 且不得 `display:none` 塌陷；`body` 不滚动而三处各自滚动；组件里不得出现硬编码颜色；图标 1.6px/无填充/继承文字色；渐变必须由令牌拼装）。改任一声明值都会即时报红，逼人重跑 `prototype/verify-prototype.js` 重新测量；**已验证守卫有牙**（把 54 改成 55 → 2 条报红，改回即绿）。测试目录另加 `node-shims.d.ts` 只声明 `readFileSync`，避免给 Web 包引入 `@types/node`。另实测 PBKDF2 600k 桌面代理值：中位数 95 ms、最慢 100 ms（风险表 #2，手机仍待测）
- v0.2.2 / 291e4fc — 去掉 `package.json` 的 BOM（上一版用 PowerShell `Set-Content` 改版本号时被写入 UTF-8 BOM；Node/pnpm 能容忍，但会让首行 diff 长期多一处噪音）。改仓库文件一律走编辑工具或显式 `UTF8Encoding(false)`，不用 shell 重定向
- v0.2.2 / fbb09cd — M1 界面路径补测（QA）修掉两个正文区丢内容的问题：① **切到「仅预览」再切回「分屏/仅编辑」时，编辑器用打开条目那一刻的快照重新挂载**——中间敲的内容在编辑器里被回退，再敲一个字就把旧内容写进草稿（真丢数据）；改为用实时文本初始化。② **切换条目时正文区没有按条目重挂载**，会沿用上一篇的文本；改为 `key={item.id}`。新增 3 个回归用例（用 CodeMirror 受控替身，jsdom 里确定性复现）。另外在真实 dev 栈上端到端验了改密链路 10 项断言：首位注册 owner、旧密码登录、改密（两个密钥同盐派生）、**改密后当前会话仍有效**、旧密码被拒、新密码可登、注册开关关/开的行为分别正确
- v0.2.1 — 站点图标（用户提供的 SVG）：新增 `apps/web/public/icon.svg` 并在 `index.html` 挂 `rel="icon"`；渐变 id 加 `mn-` 前缀（避免将来内联进页面时与其它 SVG 的 id 撞车），补 `role="img"` 与 `<title>`。只上 SVG 形态：现代浏览器都支持；iOS 主屏与 PWA manifest 需要的 PNG 等 M6 接 PWA 时一并生成
- v0.2.0 / 0425225 — **wiki 收口同步（用户确认后修改）**：项目架构 §15.5 删除 `secrets.required` 写法（deploy 硬门禁）改为运行时 `config-guard` 检查、§6.x 补全 prelogin 确定盐口径与公开注册状态接口、§2.3 目录树补 `config-guard`；local-dev 指南更正 `config-guard` 端点范围、新增 §九「本地数据与安全上下文」（重置本地数据在 `apps/web/.wrangler/state`、必须 https 或 localhost、线上诊断三连）；架构文档版本 v1.9 → v1.10
- **v0.2.0 — M1 收口（核心闭环完成）**：注册登录 → 建笔记 → 编辑保存 → 第二台设备同步看到，全线闭环并在真实浏览器与云端验证。
  - **功能**：认证（浏览器 PBKDF2 600k 派生、服务端只做一次 HMAC 比对、会话 Cookie、CSRF、实例级注册开关与 owner/member）、笔记最小集（新建/标题/正文/自动保存/大小档位与硬上限）、同步最小版（D1 游标增量、outbox 条件批写、退避重试、失败列表语义、冲突副本 + 提示）、最小设置入口（通用/账户与安全/实例管理）。
  - **质量**：`lint`/`typecheck`/`test`/`build`/`check:size` 全绿；测试 **163 个**（shared 29 + web 77 + worker 57）；首屏 JS gzip **113.1 KB ≤ 200 KB**（编辑器与 Markdown 渲染各自独立分包）。
  - **验证**：dev 栈 17 项接口断言；真实浏览器全链路走查（首位注册 → 建笔记 → 打字 → 同步 → 第二客户端拉到数据 → 断网编辑 → 恢复补传）；HAR 口径（空闲 0 请求、写一次 1 个 PUT）；云端 `me.861306.xyz` 三项核对（health 200 / 空库 / 机密生效）。
  - **收口动作**：M1 计划标记完成并移入 `docs/archive/`；未做项集中记入 M2 计划 §1.2「M1 遗留清单」；三份设计稿版本号与修改记录对齐；`AGENTS.md` 项目简介更新。
- v0.1.16 / 187fe41 — **http 打开时不再抛 TypeError**：非安全上下文没有 WebCrypto，改为给出「请改用 https 打开」的可行动错误 + 常驻红色横幅
- v0.1.15 — M1-11 补完本地可验的验收项并修一个口径缺口：① **服务端不可达演练**（真的停掉 dev 服务）——请求失败后队列项留队、`retries` 递增、错误原因入 `last_error`，**草稿 72 字符未丢**，胶囊显示"同步失败"、状态栏"待上传"；服务端恢复后按退避自动补传成功（本地清空、服务端正文含离线期间敲入的内容），期间页面重载过一次也没丢；② **停在失败列表的条目原先显示"待上传"**（`上传失败` 状态在 UI 里是死代码）——改为按队列项的哨兵时间判定并显示"上传失败"（M1 只提示，重试界面属 M2）；新增 `listItemOutbox` / `isParkedOutboxRow` 与 2 个用例（失败态、状态栏文案）。浏览器 console 全程无未处理异常
- v0.1.14 — 收窄机密门禁范围：`configGuard` 只挂在**真正要用 `AUTH_PEPPER`** 的四个端点（`/api/auth/prelogin`、`login`、`register`、`password`），不再整条 `/api/auth/*` 与 `/api/admin/*` 通拦。原因：首次部署忘配机密时，前端读不到 `registration-state` 就会停在登录页且**没有注册入口**，用户只看到一句 503 而不知道该做什么；现在仍能读到"库中没有用户"并被送到注册页，提交时才给出明确原因。实测（本地移走 `.dev.vars` 模拟）：`/api/health` 200、`/api/auth/registration-state` 200、`prelogin`/`login` 503。新增 1 个用例（缺机密时注册状态/会话/健康检查仍可用）
- v0.1.13 / 24ef819 — **M1-11 真实浏览器走查修掉两个真 bug**（agent-browser + HAR 抓的硬证据）：① **请求风暴**——启动同步引擎的 effect 依赖 `refreshAll`，而它会 `setItems(新数组)` → 重渲染 → 依赖身份变化 → 引擎反复 stop/create/start，实测 10 秒 **35 次** `GET /api/sync`；改成引擎只依赖登录状态、回调经 ref 调用，并给 `useNotesWorkspace` 返回值加 memo、列表加去重。② **无限重传**——编辑器保存态只靠"引擎回调 → App → 控制器"这条链收敛，链上任何静默失败都会卡在"待上传"，而 tick 仍每 2 秒入队一次，实测 rev 从 405 涨到 483 而内容一字未改；改成**控制器每 tick 先按本地库 + `content_hash` 对齐、再决定写草稿/入队**，并给引擎回调补 `catch` 让失败可见。新增 4 个回归用例。验收（HAR 实测）：**空闲 12 秒 0 个 API 请求**、写一次正文只发 **1 个 PUT**、顶栏胶囊与正文状态栏一致；另外走通了首位注册→建笔记→打字→同步→第二台设备（全新浏览器 profile）登录后拉到数据
- v0.1.12 / a4a5509 — **修复部署失败**：`wrangler.jsonc` 的 `secrets.required` 是 deploy 的**硬门禁**（机密未设置时 `wrangler deploy` 直接报 `The following required secrets have not been set: AUTH_PEPPER` 并失败），而首次部署时 Worker 尚不存在、无法先设机密 → 一键部署与 Workers Builds 被永久堵死（用户实测几次构建均失败于此）。改法：`wrangler.jsonc` 不再声明该字段；新增 `middleware/config-guard.ts`，缺 `AUTH_PEPPER` 时 `/api/auth/*`、`/api/admin/*` 返回 503 并说明缺什么，**绝不用空密钥算 HMAC**（fail-closed）；新增 3 个用例；`npx wrangler deploy --dry-run` 已通过（不再出现 required secrets 报错，绑定显示 `env.DB (menote-db)` + `env.ASSETS`）。**首次部署成功后需在 Dashboard 给 Worker 添加 `AUTH_PEPPER` 机密，认证接口才会可用**
- v0.1.11 / 71054f6 — M1-10 界面：`app/` 落骨架与通用件（令牌层从原型取值并标注"临时值待 DESIGN §3 定型"、Icon sprite、Controls、Menu、Toast、hash 路由、AppShell、**顶栏 6 块位**（搜索占位禁用并说明原因、未启用隐私锁不渲染胶囊）、功能栏**新建按钮 38px + 录入框 136px** 保住导航 y=252 不变量（录入框为占位：可输入与切模式，发布禁用并说明原因）、useTheme 三档且切主题不重渲染、useSyncStatus）+ `features/`（auth：浏览器 PBKDF2 派生登录密钥、登录/注册页；notes：列表、正文区、**编辑器与 Markdown 渲染各自动态 import 成独立分包**、自动保存接线；settings：三分类壳 + 通用/账户与安全/实例管理）；**修复注册与改密的盐不一致缺陷**（服务端改用与 prelogin 同源的确定盐，否则注册或改密后立刻无法登录；同时让 prelogin 不再泄露用户名是否存在）；新增公开接口 `GET /api/auth/registration-state` 供登录页决定注册入口；新增 13 个 UI 组件测试（RTL + jsdom）
- v0.1.10 — M1-10 前置：新增《Menote M1 界面稿》（`docs/modules/`，逐屏列出 M1 六屏的块/主操作/空状态/键盘底线与 M1 不做清单，按 AGENTS.md「先稿后码」待人确认）；把设计稿里标着"仍未验"的 dev 链路实测掉——真实 `pnpm dev`（Vite + workerd + 本地 D1）上跑 17 项断言全过（Cookie 属性无 `Secure`、CSRF 两路 403、注册首位 owner、条目新建/取正文/304/保存/409 带服务端哈希、拉取可见 rev=2、文件夹、注册开关、第二位用户为 member、他人条目 404），并把"真实浏览器 Cookie 存储"这一仍未验项如实留到 M1-11
- v0.1.10 / dd235b6 — M1-9 编辑器：`app/editor/`（**save-policy**：本地草稿恒 2 秒、上传空闲 2 秒 / 大文档 5 秒、持续输入最长 30 秒 / 大文档 60 秒、硬上限只挡上传不挡草稿；**markdown**：markdown-it `html:false` + DOMPurify + 外链 `rel="noopener noreferrer"`；**Editor**：CodeMirror 6 封装，只在挂载时创建 `EditorView`、只读走 `Compartment` 重配置，切模式不重建文档）+ `features/notes/`（**model**：自动保存控制器把"草稿 → 入队 → 通知同步引擎"串起来，时间可注入；**ui/DocStatusBar**：大小、软/硬上限提示常驻可见、已同步/待上传/上传失败/冲突四态，语义靠文字不靠颜色）；引入 CodeMirror 6 / markdown-it / DOMPurify（测试用 jsdom）；新增 21 个用例（策略边界、控制器状态机、渲染与 XSS 防线、外链 rel）
- v0.1.9 / 5e96513 — M1-8 同步引擎：`data/sync/` 四件套——**push**（按 op 分派 create/save_body/patch_meta/create_folder/patch_folder，正文读 `drafts`；上传成功后**只清未被改动的草稿**，否则立刻再排一次保存，避免飞行期间输入丢失；409 分两路：哈希与本地一致→视为上次已成功，不同→本地内容另存**冲突副本**并让原条目采纳服务端版本；422/403/404 移入失败列表（哨兵 `next_retry_at`，不阻塞队列）；网络错误按 1/2/4/8…≤60s 抖动退避）、**pull**（循环推进游标；`full_resync` 只清已同步内容、保留未上传改动）、**leader**（Web Locks 选主，不支持则退化直跑）、**engine**（`runOnce` 编排 + 站点打开/写入成功/可见/联网/前台 5 分钟触发 + 状态回调）；新增 17 个前端用例（冲突副本、失败列表、退避序列、防抖、busy 互斥、full_resync）；同步设计稿升 v1.4 记录 §4.8 实现口径
- v0.1.8 / 5e177cf — M1-7 前端数据层：`data/api/`（传输层统一带 `X-Menote` CSRF 头、15s 超时、错误映射为 `ApiError`（网络/超时归 `retry_later`、**304 不算错误**）、不做自动重试；端点封装集中路径与请求头，拉取响应过共享 schema 校验）+ `data/db/`（Dexie v1 六张表、字段名与服务端同构；仓储实现"入站只覆盖元数据且保留本地 `pending`、正文缓存仅在哈希变化且**无待上传改动**时失效、outbox 合并保留**最早**的 `base_rev`/`base_meta_rev`、队首按 `next_retry_at`"）；`shared` 补 `sha256Hex`（`content_hash` 计算，含 FIPS 向量测试）并修正 `crypto` 最小声明（补 `subtle.digest`，此前会导致 shared 构建失败）；`apps/web` 接入 vitest + fake-indexeddb（node 环境，顺带防住误用 `window`/`document`），`pnpm test` 从此覆盖三个包；新增 22 个用例
- v0.1.7 / ed3fa94 — M1-6 增量拉取：`GET /api/sync?cursor=N` 只回元数据（items/folders 各 ≤200 行，多取 1 行判 `has_more`）；**同一 `sync_seq` 的组不被切开**（整组回退；单组超上限返回 413 而非死循环）；`next_cursor` 取两类末端较小值，**且某类没有新行时不参与 min**（否则游标原地打转，M1 实测踩到）；`full_resync` 收紧为 `cursor>0 且 < tombstone_floor`（游标 0 不死循环）；**修复 `SQL_INSERT_ITEM` 漏 `+1` 导致新条目 `sync_seq=0`、拉取永远看不到的真 bug**，并在条目用例补回归断言；新增 10 个拉取集成测试（分页不漏拉、组边界回退、软删标记、full_resync、多用户隔离、游标非法）；设计稿升 v1.3 记录两处协议口径修正
- v0.1.6 / 7e3cce0 — M1-5 条目与文件夹：条目四个端点（`PUT /api/items/:id` 新建幂等、`GET /api/items/:id/body` 带 ETag/304、`PUT /api/items/:id/body` 带 `If-Match` + `X-Menote-Hash`、`PATCH /api/items/:id/meta`）与文件夹两个端点（创建、改名/移动，层级上限 2 层）；写路径统一「预检读 → 一个 batch（主写入以 rev 为条件 + 正文带 rev/content_hash 双守卫的 upsert + 计数器条件推进）」，冲突整批无改动；SQL 常量与白名单列组装集中到 `db/tables.ts`；`shared` 补 `folders` 与 `text`（`utf8ByteLength`/`countCodePoints`，与 SQLite 的码点口径对齐）；补 M1-4 的安全响应头；**新增 16 个集成测试**（并发同基版本保存只有一个成功且落败者不覆盖正文、条件 batch 过期时正文与计数器都不动、响应丢失重放视为成功、Memo 形状约束、413 硬上限、文件夹层级与自环、多用户隔离）；设计稿《同步引擎设计》新增 §3.5 记录实现口径（文件夹接口、应答形状、服务端不重算哈希的取舍）
- v0.1.5 / d1ea369 — M1-3 认证与会话：服务层 `tokens`（令牌生成、SHA-256、HMAC、**常量时间比较**、prelogin 假盐派生）、`sessions`（建/解/销、滑动续期且每天最多写一次）、`auth`（prelogin、注册单语句判定首位 owner、登录节流、改密并使其他设备会话失效）、`settings`（注册开关走 `app_meta` 键）；中间件 `session`（`requireSession` + Cookie 读写，Secure 仅在 https 下开）与 `csrf`（`X-Menote` 主防线 + Origin 次防线）；路由 `auth`（prelogin/login/register/logout/me/password）与 `settings`（`/api/admin/registration` 仅 owner）；SQL 常量集中到 `db/tables.ts`，新增 `DomainError` 由入口统一转架构 §4.3 错误体；`shared` 补 `AuthSessionResponse` 与 **ULID 生成器**（主键时间有序）；`wrangler.jsonc` 声明 `secrets.required = ["AUTH_PEPPER"]` 并在 vitest 注入测试 pepper；新增 15 个认证集成测试（首位注册即 owner、注册开关与非 owner 拒绝、假盐确定性与大小写、节流冷却、改密失效其他会话、CSRF 两路、伪造令牌不 500、测试环境 pepper 绑定）
- v0.1.4 / 283e66e — M1-2：`packages/shared` 落协议契约——新增 base64url（无填充、两端共用、非法字符不静默丢弃）、`limits.ts`（上限与阈值常量，逐条注明来源）、`items.ts`（ItemMeta / FolderMeta / ItemWriteMeta 与 `X-Menote-Meta` 编解码）、`sync.ts`（SyncResponse / 保存应答 / 冲突 detail）、`auth.ts`（KDF 参数、用户名规则、认证请求响应）；类型一律由 Valibot schema 经 `InferOutput` 推导（schema 为唯一真相）；引入 `valibot@1.5.0`（首屏体积未受影响，仍 66.8 KB ≤ 200 KB）；`globals.d.ts` 只声明用到的 TextEncoder/TextDecoder，避免为类型引入 DOM lib 而放开浏览器专有 API；shared 单测 21 个（+17）
- v0.1.3 / a29dfb4 — M1-1 落地：运行时自愈迁移（`apps/worker/src/db/selfheal.ts`：读版本 → 抢锁 → 按序执行 → 校验对象 → 写版本，isolate 级缓存）+ 0001 首批 8 张表 / 10 个索引（新隐私模型权威 DDL，无密文列）+ `middleware/schema.ts` 表结构守卫（`/api/health` 作存活探针不依赖 D1，其余 `/api/*` 未就绪返回 503 retry_later）；新增 9 个 D1 集成测试（DDL 齐备、幂等、并发抢锁、中途失败整批回滚且版本不前移、结构漂移边界、守卫放行/拦截）；`.gitignore` 放行 `.dev.vars.example` 并新增该示例文件；AGENTS.md 补「关键阶段即时提交并推送」约定、项目简介更新为 M1 现状
- v0.1.2 / 553e1f2 — M1 评审结论回写 wiki：架构升 v1.9（§15.4/§15.5 迁移机制统一为运行时自愈、§5.1 建表指向新隐私模型权威 DDL、清理 §6.1/§6.3 的 v1.0 遗留、§15.5 机密清单删 `SESSION_SECRET`、§2.3.2 补 `/api/health` 与 `services/auth.ts` 落点并在目录树补 `middleware/`、表头版本由 v1.1 更正为 v1.9）；`wiki/guides/local-dev.md` §8 重写为「AUTH_PEPPER + 运行时自愈迁移」；功能拆解升 v2.4（Q22 定为登出不清除本机缓存、实例级设置用 `app_meta` 键）；三份 M1 专项设计稿状态改「生效」
- v0.1.2 / 553e1f2 — M1 开工前评审：新增三份专项设计（`docs/modules/` 数据模型与迁移设计、同步引擎设计、认证与会话设计），解掉四个阻塞项（迁移机制二义性与首次部署陷阱→改为运行时自愈迁移；新隐私模型缺权威 DDL→给出重写 CHECK 的权威版；同步引擎专项设计缺失；KDF/密钥/环境/设置入口口径未定）；新增 M1、M2 两份实施计划（`docs/todo/`）；开发计划升 v1.2（M04-02→M04-04/M04-05 勘误、M1 表清单补齐、M1 自带最小设置入口、界面骨架一次成型、DESIGN 视觉章时点、墓碑归 M4、Memo 图片跨里程碑边界）。定稿前用 SQLite 实测校正 DDL 与条件写模式（18 条语句、CHECK 正反例、upsert 守卫、游标取 min），据独立复核修正 20 余处，并记下三项需用户点头的发现：需求 §18.3 计数器守卫重放缺陷、`.gitignore` 屏蔽 `.dev.vars.example`、`site_settings`/登出清缓存两处定稿冲突
- v0.1.2 / 40e5113 — 评审反馈批次修复（合并记一次 +0.0.1）：run_worker_first 补 "/api"（无尾斜杠会落 SPA 回退返回 HTML）；wrangler.jsonc 显式 migrations_dir（插件默认改写到不存在的 <根>/migrations）并建 apps/worker/src/db/migrations/；ESLint 补依赖方向护栏（routes/services/db 分层 + packages 禁依赖 apps）；删除误用的 web deploy 脚本；CHANGELOG 归属勘误
- v0.1.1 / aede7bf — AGENTS.md 与 DESIGN.md 增补「移动端兼容」约定：移动端后续会做，窄屏布局定稿前不得留下仅桌面成立的写法（弹性 / 流式布局、不以悬停为唯一入口、触屏命中区、允许视口缩放、滚动仍每层一个容器）；DESIGN.md 升 v1.1 并增禁止项第 17 条
- v0.1.1 / 4f05411 — 修复 Workers Builds 部署失败（assets 缺 directory）：build 末尾在仓库根生成 .wrangler/deploy/config.json 指针，默认 `npx wrangler deploy` 即采用 Vite 产物配置；guide §6 部署说明同步更新
- v0.1.0 / 5fce691 — 版本号规范落进 AGENTS.md（主版本号仅用户主动变更；次版本号随里程碑 +0.1；修订号 +0.0.1，分批可合并）；根 package.json 同步为 0.1.0
- v0.1.0 / 7a8d42b — 重写 README：特性与技术栈（含已接入/规划状态）、快速开始、目录结构、常用命令、部署（Deploy 按钮 + Workers Builds）、文档导航与冲突优先级
- v0.1.0 / 0d26862 — 首篇操作指南 wiki/guides/local-dev.md（本地开发上手 + 部署链路）；README「怎么跑」更新
- v0.1.0 / 5b371c6 — M0 工程骨架：pnpm workspace（apps/web + apps/worker + packages/shared）、Worker /api/health、Vite 8 + React 19 前端、wrangler 部署链路（D1 自动供给 + SPA 静态资源 + CSP 头）、CI（lint / typecheck / 测试 / 构建 / 体积检查）
- 预开发 — 新增 docs/todo/Menote-开发计划-v1.md（前置准备评估 + M0-M6 里程碑规划，草案待确认）
- 预开发 / 1cc0ec5 — 文档规划指南 v1.1：补 API 与接口文档规划
- 预开发 / 9268ac1 — DESIGN.md 首稿：结构 / 交互 / 用法已定（布局尺寸、间距原则、组件用法与层级、交互与状态、无障碍底线、16 条禁止项），视觉语言整章待定；components.md 同步令牌引用
- 预开发 / f3f3585 — components.md 补 SegmentedControl 的用法交叉引用（ComposerModeTabs / DocModeSwitch 等同族，非独立控件）
- 预开发 / 0bc6d80 — 新增 wiki/components.md 组件规划（组件清单 + 落点 / 职责 / props / 复用 / 原型对应，附功能点映射矩阵与预留组件）
- 预开发 / aba789e — 新增 wiki/guides/docs-roadmap.md 文档规划指南（通用模板 + 本项目落地对照）
- 预开发 / 5f2f7e4 — deliverables/ 评审产物不入库，加入 .gitignore（修正误加）
- 预开发 / ca3674e — 按文档约定迁移主线文档至 wiki/ 并归档功能拆解 v1，新增 README（架构 v1.8）
- 预开发 / 2c61a80 — CHANGELOG 改为日期小标题格式（AGENTS.md 规则同步）
- 预开发 / b38fb20 — 草稿改为 docs/scratch/ 文件夹（一份讨论一份文件），架构升 v1.7 与 AGENTS.md 同步
- 预开发 / f68de6e — CHANGELOG 补记 scratch 目录化
- 预开发 / b24a31a — AGENTS.md 文档放哪补充 todo/ 与 scratch.md，创建对应目录与草稿文件
- 预开发 / cdeb147 — 架构 v1.6：docs/ 新增 todo/ 专项计划目录与 scratch.md 草稿区
- 预开发 / 9ad1462 — AGENTS.md 填充项目信息并修正模板残留（server.mjs 改为 Worker 入口规则），CHANGELOG 建立
- 预开发 / 45b5eb4 — 架构 v1.5：文档体系对齐 AGENTS.md（wiki/ 定稿区 + docs/ 过程区两层模型）
- 预开发 / c2908e9 — 架构 v1.4：docs/ 文档体系定稿（组件规划、方案草稿、ADR、归档目录与生命周期规则）
- 预开发 / 4cb26fb — 架构 v1.3：代码组织定稿（入口只装配规则、功能→代码落点对照表、防膨胀护栏）
- 预开发 / c4f6b47 — 架构 v1.2：一键部署到 Cloudflare（资源部署时自动创建绑定），发布主路径改为 Workers Builds
- 预开发 / 8cf0fe4 — 架构 v1.1 残留清理：目录树、Dexie 注记、R2 e/ 前缀、永久删除与分包列表与新隐私模型对齐
