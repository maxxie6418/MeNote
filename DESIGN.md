# Menote 界面与交互规范（DESIGN.md）

| 项 | 内容 |
|---|---|
| 文档性质 | 界面与交互规范。**本项目唯一视觉源**：视觉语言、布局与密度、组件用法、交互与状态、无障碍底线、禁止项 |
| 权威级别 | 全局规则。实现界面时先读本文；本文未覆盖的从原型取，并把结论回写本文 |
| 基准 | 原型 `prototype/menote-prototype.html`（高保真）、`prototype/menote-framework.html`（线框评审页）；组件清单、落点与 props 看 `wiki/components.md`；功能规则看 `wiki/Menote-设计文档-v7.4.md` 与 `wiki/Menote-功能拆解-v2.md`；实现分层看 `wiki/Menote-项目架构-v1.md` |
| 版本 | v1（文件名 `DESIGN.md` 不变，版本在修订记录内演进） |
| 日期 | 2026-09-26 |
| 状态 | **首稿·部分待定**。第一、二、四、五、六、七、八章已定；**第三章「视觉语言」待定**——配色、字体、字号刻度、圆角刻度、阴影均未定型，见 3.3 |
| 不包含 | 功能该不该做（归需求文档 / 功能拆解）；组件有哪些、放哪、什么 props（归 `components.md`）；接口、数据结构、部署（归架构文档） |

### 标注约定

| 标注 | 含义 |
|---|---|
| 【已定】 | 已确认，实现时必须遵守 |
| 【待定】 | 尚未决定。本节只写现状与待补范围，实现前需用户确认 |
| 【禁止】 | 硬性禁止，违反即视为不合规 |
| 【实测】 | 从原型量出的基线值（回归脚本或探针测得），**改动相关区域前须重新测量** |

### 修订记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1 | 2026-09-26 | 首稿。定布局骨架与尺寸、间距原则、组件用法、交互与状态、无障碍底线、禁止项；视觉语言（配色 / 字体 / 字号 / 圆角 / 阴影）整章留待定 |

---

## 〇、本次定稿的边界（先读这一节）

本文刻意分成两层：

| 层 | 章节 | 状态 | 换视觉方向时 |
|---|---|---|---|
| **结构 / 交互 / 用法** | 一、二、四、五、六、七、八 | 【已定】 | **基本不变** |
| **视觉语言** | 三 | 【待定】 | 整章重写 |

**为什么这么分**：目前不打算固化「Claude 橙白暖色」这套观感。原型当前采用它，但它**只是当前实现参考，不构成规范**，可以整体替换。因此本文先落「与观感无关、但缺了就没法开工」的部分：布局骨架、尺寸、间距原则、组件用法与层级、交互与状态、无障碍底线、禁止项。等视觉方向定了，只需补第三章。

**因此本文现在不回答**：用什么颜色、用什么字族、字号多大、圆角多大、阴影多重。这些一律见 3.3 的待定清单。

**相应地，两个已知耦合必须在换视觉方向时一起处理**（详见 3.4）：

1. `prototype/verify-prototype.js` 把 Claude 色板写成了断言（`--bg:#faf9f5`、`--primary:#d97757`，以及一条 6 个色值的蓝色黑名单）。改配色时这个脚本会先失败。
2. 原型内有 **14 处**硬编码渐变色（走令牌的仅 3 处），**不参与**上述蓝色黑名单扫描（`linear-gradient()` 里的色值扫不到）。

---

## 一、唯一视觉源与文档分工【已定】

| 问题 | 看哪里 |
|---|---|
| 界面长什么样、怎么交互（本文职责） | **本文** |
| 有哪些组件、放哪目录、什么 props、怎么复用 | `wiki/components.md` |
| 这个功能该不该有、规则是什么 | 需求文档 + 功能拆解 |
| 数据存哪、接口怎么调、怎么部署 | 架构文档 |

**冲突优先级**：功能与规则看需求文档；长什么样看本文与原型；某一屏的结构看 `docs/modules/` 对应专项；组件契约看 `components.md`。

【禁止】

1. 另立第二套视觉规范（新增颜色 / 字号 / 间距体系而不同步本文）。
2. 引入 UI 组件库（Ant Design / MUI / shadcn 等）。
3. 为单个功能另做一套页面骨架（顶栏 + 左右两栏是全局骨架，不得绕过）。
4. 单功能自造视觉语言（自定圆角、自定阴影、自定按钮样式）。

---

## 二、布局骨架与尺寸【已定】

### 2.1 骨架

```
┌──────────────────────────────────────────────────────┐
│ 顶栏（全宽，6 块，固定高）                            │
├──────────────┬───────────────────────────────────────┤
│ 功能栏        │ 主操作区                              │
│ （固定宽）    │  ├─ 左列：条目列表（固定宽）          │
│  新建按钮     │  ├─ 右列：正文（自适应，弹性）        │
│  快速录入框   │  └─ 侧滑详情（绝对定位浮层，可选）    │
│  导航区(滚动) │                                       │
│  加密空间(贴底)│                                      │
└──────────────┴───────────────────────────────────────┘
```

- 高度 `100vh`，**不整页滚动**；滚动发生在各栏内部（见 2.7）。
- 左列与右列宽度固定、右列弹性。整页无水平滚动。

### 2.2 结构尺寸表【已定】

这些是**结构尺寸**（决定构图），不是观感参数，换视觉方向时不变。已抽为令牌的以令牌名为准。

| 区域 / 元素 | 值 | 备注 |
|---|---|---|
| 顶栏高 `--topbar-h` | 54px | 固定，不随内容变化 |
| 功能栏宽 `--fnbar-w` | 294px | 固定 |
| 列表列宽 `--list-w` | 330px | 双栏视图的左列 |
| 侧滑详情宽 | 390px（≤1080px 时 330px） | 绝对定位浮层 |
| 设置左列分类导航宽 | 184px | 设置页内部两栏 |
| 快速录入框输入区高 | 最小 40px / 最大 180px | 可纵向 resize |
| 快速录入框模式附加项高 | **固定 26px、不换行** | 越界横向滚动，不允许撑高 |
| 控件高：按钮 | 31px（`sm` 27px；正文状态栏内 21px） | |
| 控件高：分段控件按钮 | 25px（录入框内的模式切换 23px） | |
| 控件高：状态胶囊 `pill` | 30px | 同步状态 |
| 控件高：隐私胶囊 `capsule` | 32px | |
| 控件高：胶囊 `chip` | 21px（录入框附加项 `sw` 20px） | |
| 控件高：复选框 | 17px | |
| 控件高：开关 `toggle` | 38×21px | |
| 控件高：输入框 | 38px（顶栏搜索框 33px） | |
| 头像 | 29px（顶栏 30px；菜单头 26px） | |
| 图标按钮 | 24×24px | |
| 列表行类型图标底 | 26×26px | |
| 缩略图：书封 / Memo 图 | 28×38px / 74×74px | |
| 弹窗最大宽 | 400px | |
| 占位图大图标底 | 50×50px | |

### 2.3 内容宽度上限【已定】

正文与卡片区**必须有宽度上限**，避免宽屏下长文行宽过长。四个上限按用途区分：

| 用途 | 上限 |
|---|---|
| Markdown 正文 / Memo 时间轴 | 740px |
| 设置页卡片组 | 790px |
| 首页卡片组 | 920px |

### 2.4 断点【已定】

| 断点 | 变化 |
|---|---|
| ≤1240px | Memo 瀑布流 3 列 → 2 列 |
| ≤1080px | 待办看板 3 列 → 1 列；侧滑详情 390 → 330px |

**窄屏 / 移动端布局【后续定】**（功能拆解 M02-01）。在它定下来之前，**不要**为移动端临时发明布局。

### 2.5 结构不变量（改动前必查）【实测】

1. **顶栏固定 6 块**：品牌 · 面包屑 · 搜索框 · 同步胶囊 · 隐私锁胶囊 · 账户与设置。数量与顺序都不可增减重排（`verify-prototype.js` 有 `children.length === 6` 断言）。
2. **快速录入框总高 136px、其下方导航区顶部 y = 252px 不变**。切换录入模式**只换内容不换高度**，绝不推挤下方导航。附加项容器必须「固定高度 + 不换行」，**不得**用 `display:none` 让容器塌陷。
3. **加密空间贴底固定**，不随导航区滚动，且不设分组小标题。
4. **两条导航造型不得合并**：浏览三段是**下划线页签**（无框无底），录入框模式切换是**盒式分段控件**（外框 + 灰底选中）。两者上下相邻，改动任一方都要重新确认区分还在。
5. **功能栏内不出现账户区**（账户入口在顶栏）。

### 2.6 主操作区的排布方式【已定】

| 排布 | 用在 | 实现 |
|---|---|---|
| 列表 + 正文双栏 | 笔记本 / 加密空间 / 最近编辑 / 收藏 / 标签 | 左列 330px + 右列弹性；点条目**在右列直接打开** |
| 单栏占满 | 搜索结果 | 列表列切换为 `wide` 模式 |
| 整块隐藏 | 加密空间锁定时 | 列表列整块隐藏，右列给锁定占位 |
| 主操作区独立页 | 首页 / Memo / 待办 / 表格 / 回收站 / 设置 | 不用左列，右列占满 |

### 2.7 滚动归属【已定】

**每层只允许一个滚动容器**，禁止嵌套双滚动。

| 层 | 滚动行为 |
|---|---|
| 页面 `body` | **不滚动**（`overflow:hidden`） |
| 功能栏 | 仅「导航区」滚动；**新建按钮 + 快速录入框固定在上，加密空间固定在底** |
| 列表列 | 自身滚动 |
| 正文列 | 正文头固定、底部状态栏固定、**中间正文区滚动** |
| 设置页 | 左列分类导航独立滚动；右侧内容区独立滚动 |

---

## 三、视觉语言【待定】

### 3.1 声明

**本章尚未定型。** 原型当前采用一套「暖色橙白 + 双主题」观感（Claude 风格），它**只是当前实现参考，不是规范**，可以整体替换，且替换时不视为破坏约定。

**在 3.3 补齐之前**：界面实现所需的颜色 / 字体 / 字号 / 圆角 / 阴影，一律从原型取值，并在实现处标注「临时值，待 DESIGN §3 定型后替换」。

### 3.2 现在就生效的机制（与具体色值无关）【已定】

这部分是「怎么用视觉」，不是「视觉是什么」，因此现在就要遵守：

1. **一切颜色走令牌**。禁止在组件里写十六进制、`rgb()`、颜色关键字。包括条纹填充、遮罩、阴影、反白文字（`--on-primary` 一类），全部必须令牌化——**没有例外**。
2. **禁止硬编码在渐变里**。`linear-gradient(...)` 中的色值**不会**被回归脚本的扫描命中，是本项目已发生过的漏网点（文字 logo、新建按钮、头像、Memo 缩略图、瀑布流图、图册封面）。渐变必须由令牌拼装。
3. **双主题**：浅色与深色两套令牌必须成对提供，且**深色不是浅色的机械反转**——深色下需单独核对语义色可读性。切换只改主题属性，**不得整页重新渲染**（会丢失滚动位置与输入焦点）。
4. **语义色不得单独表意**。状态（同步中 / 失败 / 冲突 / 加密 / 已完成）必须以**文字或图标**表达，颜色只是辅助——否则色觉障碍用户与深色主题下会失去信息。
5. **主题三档**：浅色 / 深色 / 跟随系统，默认浅色。跟随系统时必须响应系统主题变化；在无 `matchMedia` 的环境（如测试环境）回退为浅色，**不得抛错**。

### 3.3 待定清单（视觉定型时一次性补）

| 项 | 待定内容 | 现状 |
|---|---|---|
| 配色 | 主色、语义色（成功 / 警告 / 危险 / 中性）、底色层级、边框色、文字三级色、遮罩、阴影 | 原型有一套暖色橙白双主题，可整体替换 |
| 字族 | 正文无衬线字族、等宽字族（行号 / 时间 / 尺寸 / 键帽用） | 原型用系统字族栈 + 等宽栈 |
| 字号刻度 | **必须收敛为有限刻度**（见 3.4 问题 1） | 原型有 15 个字号值，含 0.5px 递增，无刻度 |
| 圆角刻度 | **必须收敛为 2–4 档**（见 3.4 问题 2） | 原型有 17 种圆角写法 |
| 阴影 | 层级数（原型为 3 档）+ 各自用途 | 原型 `--shadow-1..3` |

### 3.4 定型时必须一并解决的问题

**问题 1 · 字号无刻度。** 原型实际使用 15 个字号值（10 / 10.5 / 11 / 11.5 / 12 / 12.5 / 13 / 13.5 / 14 / 14.5 / 15 / 16.5 / 17 / 20 / 21px），其中 11.5 / 12.5 / 13.5 是最高频值，却与相邻档只差 0.5px——肉眼几乎不可辨，却让「哪一级更重要」失去意义。定型时应收敛为约 6 档并给出语义名（如 标题 / 小标题 / 正文 / 次要 / 辅助 / 微标）。

**问题 2 · 圆角无刻度。** 原型使用 17 种圆角写法（3/4/5/6/7/8/9/10/11/15/16px、50%、99px，另有 2 个令牌与复合值如 `2px 2px 0 0`），同类控件（按钮、卡片、胶囊）之间不一致。定型时应收敛为 2–4 档。

**问题 3 · 回归脚本与色板耦合。** `prototype/verify-prototype.js` 内写死了 `--bg:#faf9f5`、`--primary:#d97757` 以及一条 6 值蓝色黑名单。**换视觉方向时该脚本必须先改**，否则会在无关改动上失败，形成假信号。

**问题 4 · 渐变漏网。** 见 3.2 第 2 条。定型时把 14 处硬编码渐变收成令牌（走令牌的只剩 3 处），并在回归脚本里补一条「扫描 `linear-gradient` 内色值」的检查。

---

## 四、间距与密度【已定·原则；刻度待定】

### 4.1 原则【已定】

1. **单一刻度**：所有内外边距与间隙必须取自同一套刻度，不允许随手写数值。
2. **按关系定档**，而不是按位置：
   - **紧凑档**：同一控件内部（图标与文字、标签与标签）
   - **常规档**：卡片 / 列表行内部
   - **宽松档**：区块之间、面板边缘
3. **相同层级的元素，间距必须相同**。同一屏里两个并列卡片的内边距不一致即视为不合规。
4. **间距优先用父容器的 `gap`**，避免给子元素逐个加 `margin`（后者在换向 / 换行时容易出问题）。

### 4.2 现状问题【待定】

原型实测出现 **31 种不同间距值**（1–20px 连续无缺，另加 22/24/25/26/28/32/34/40/52/60/80px），且 6/7/8/9px 与 11/12/13/14px 各组内部混用严重——**没有网格**。这是 4.1 第 1 条要解决的问题。

### 4.3 具体刻度【待定】

待视觉定型时一并确定（建议 4px 或 2px 基数），届时本节给出数值表，并说明三档密度各对应哪一档刻度。**在此之前不要新增自定义间距值**——从原型取值时优先复用已有的高频值。

---

## 五、组件用法与层级【已定】

组件清单、落点与 props 见 `wiki/components.md`。本节只规定**什么时候用哪个**。

### 5.1 操作层级（同一屏内）【已定】

每个界面区域**最多一个主操作**。

| 层级 | 样式 | 用在 | 一屏数量 |
|---|---|---|---|
| 主操作 | 实心主色按钮 | 该区域唯一的推进动作（发布、解锁、重置并解锁） | **最多 1 个** |
| 次操作 | 默认描边按钮 | 常规动作（修改、调整、撤销、管理） | 若干 |
| 弱操作 | 幽灵按钮 | 图标型、低权重、工具栏内 | 若干 |
| 危险操作 | 危险色文字 + 危险色描边 | 删除、清空、永久删除、关闭保护 | 按需；**必须二次确认** |

【禁止】同一区域出现两个并列的实心主色按钮。

### 5.2 密度与尺寸【已定】

按钮三档尺寸与场景绑定：默认档用于页面与面板；紧凑档用于工具栏、列表行、卡片头；最小档仅用于**正文底部状态栏**这类极致紧凑的场景。**不要**为了「看起来整齐」把页面主按钮降到紧凑档。

### 5.3 控件选型（易错，逐条对照）【已定】

| 场景 | 用 | 不用 | 原因 |
|---|---|---|---|
| 互斥的**视图切换**（表格/图册、时间轴/瀑布流、列表/看板、编辑/分屏/预览） | 盒式分段控件 | 下划线页签 | 需要有「当前在哪一档」的强选中感 |
| 顶部**同级导航**（首页/Memo/待办） | 下划线页签 | 盒式分段控件 | 它是导航不是参数；且必须与紧邻的录入框模式切换区分开 |
| 互斥的**配置选项**（启动视图、主题、解锁档位、默认编辑模式） | 单选组（带标题 + 说明） | 分段控件 | 选项需要解释文字 |
| 开 / 关 | 开关 | 复选框 | |
| 列表项的**多选 / 完成勾选** | 复选框 | 开关 | |
| 轻量**分类标记**、可点筛选（全部/待办/进行中/已完成） | 胶囊 `chip` | 按钮 | 视觉权重更低 |
| **条目属性**（标签、截止、优先级、状态） | 胶囊 `chip` | 纯文字 | 需要边界感，且要能一眼分组 |
| 顶栏**状态**（同步中 / 已同步） | 状态胶囊 `pill` | 普通胶囊 | 带语义色，位置固定在顶栏 |
| 全局**隐私锁状态** | 隐私胶囊 `capsule` | 状态胶囊 | 可点击、有倒计时、有更重状态 |
| 录入框**模式附加项**（截止/优先级/首行作标题） | 可选胶囊 `sw` | 胶囊 `chip` | 语义不同：`sw` 可切换、`chip` 只展示。**二者不得合并**，但底层共用同一套胶囊外观 |
| 锚定在某个元素旁的**短菜单** | 菜单浮层 | 弹窗 | 弹窗用于需要输入或需要明确阅读的场景 |
| 需要**输入 / 确认 / 阅读后果** | 弹窗 | 菜单 | |
| **属性查看**（不改主流程） | 侧滑详情 | 弹窗 | 但侧滑详情当前**不由列表触发**，见 6.7 |
| **操作结果反馈** | 轻提示 | 弹窗 | |

【禁止】用原生浏览器弹窗做确认或提示。

### 5.4 文案与表意【已定】

1. **界面辅助文案默认不写**。确需的说明性文字（用途、口径、背景）一律收进 `InfoHint`（ⓘ + 悬停），不得平铺在界面上。
2. **但下列内容必须保持可见**，不得藏进 `InfoHint`：警告、破坏性操作的后果、错误与校验提示、实时计数。
3. **空状态必须给出口**。任何空列表 / 空搜索结果 / 空回收站都要有「为什么空 + 下一步做什么」，不能只显示一行灰字。
4. **锁定状态必须如实说明保护边界**。本项目隐私模型是「服务端明文 + 前端门禁 + 备份导出加密」，因此锁定态文案**不得**使用「已加密存储」「内容已加密」这类误导表述，必须说明：不渲染、不进搜索，但存储方式不变。
5. **不可逆操作的确认框必须写明影响范围与数量**（例：「文件夹及其中 N 条内容、M 个子文件夹将移入回收站」）。
6. **术语统一**：加密相关一律用「**加密空间**」，不用「隐私空间」。密码分「登录密码」与「隐私密码」，两者独立，文案不得混用。
7. **不用 emoji 作图标**，不用 emoji 作状态标记。

### 5.5 图标【已定】

1. 图标一律走 SVG symbol sprite，**不内联 path、不引外部图标库、不用字体图标**。
2. 尺寸三档：常规 16px、小 13px、大 20px。与文字并排时对齐文字光学中线。
3. 描边规格统一（当前：1.6px 描边、无填充、`currentColor`），**颜色继承文字色**，不为图标单独设色（语义色除外）。
4. 图标不可作为唯一表意手段——必须配文字或 `aria-label`。

---

## 六、交互与状态【已定】

### 6.1 状态全集

每个可交互元素必须显式定义以下状态，缺一视为未完成：

| 状态 | 要求 |
|---|---|
| 默认 | — |
| 悬停 | 必须有可见变化 |
| 焦点 | **必须可见**（见 6.2）。原型当前仅在 3 个输入框上有焦点环，按钮无焦点样式——实现时必须补 |
| 激活 / 选中 | 必须能一眼看出「当前在哪一项」 |
| 禁用 | 降低不透明度 + 禁止指针；**且必须说明为何禁用**（悬停提示或旁注），不可只置灰 |
| 加载中 | 有进度或骨架，禁止静默等待 |
| 空 | 见 5.4 第 3 条 |
| 错误 | 就地提示，不弹窗（弹窗仅用于需确认的场景） |

### 6.2 焦点与键盘可达（底线）【已定】

原型几乎未做无障碍（全文件仅 1 处 `role="button"` + 1 处 `tabindex="0"`，无 `:focus-visible`，无 `prefers-reduced-motion`）。实现时按以下底线补齐：

1. **所有可点击元素必须键盘可达**，且**焦点可见**（`focus-visible` 焦点环，不得用 `outline:none` 抹掉）。
2. 自定义控件必须补齐语义：可点击的 `div` 一律改为 `button`，或用 `role` + `tabindex` + 键盘事件（Enter / Space 触发）。
3. 弹窗与菜单需焦点管理：打开时焦点移入、`Esc` 关闭、关闭后焦点归还触发元素。
4. 图标按钮必须有 `aria-label`（原型用 `title`，不足够）。
5. 纯装饰图标对辅助技术隐藏（`aria-hidden`）。
6. 尊重 `prefers-reduced-motion`：用户开启减弱动效时，取消位移与缩放类动画（保留极短淡入）。
7. 文字与背景对比度不低于 WCAG AA。

### 6.3 快捷键总表【已定】

| 按键 | 作用 | 生效范围 |
|---|---|---|
| `Ctrl` / `Cmd` + `K` | 聚焦顶栏搜索框 | 全局 |
| `Ctrl` / `Cmd` + `Enter` | 发布录入框内容（按当前模式：Memo / 待办 / 笔记） | 录入框内 |
| `Enter` | 提交解锁弹窗 | 解锁弹窗内 |
| `Esc` | 依次关闭：下拉菜单 → 解锁弹窗 → 重置密码弹窗 → 侧滑详情 | 全局 |

规则：

1. 「主操作 + Enter」是本项目统一的提交手势，新增加的需要提交的输入区应遵循。
2. `Esc` 是**统一的退出**手势，新增浮层必须接入。
3. 快捷键提示写在对应按钮的 `title` 上，**不占独立行**（避免撑高布局）。

### 6.4 浮层行为【已定】

1. 菜单锚定在触发元素旁，**空间不足时自动向上翻**，且不越出视口边缘。
2. 点击浮层外部关闭；点击浮层内部不关闭。
3. 菜单内嵌套的**分段控件**（如菜单里的主题三档）需自行接管点击，**切换后不关闭菜单**。
4. 同一时刻只允许一个菜单打开。
5. 弹窗点击遮罩关闭；`Esc` 关闭。

### 6.5 破坏性操作【已定】

必须二次确认的操作：删除 / 永久删除 / 清空回收站 / 关闭隐私锁 / 重置隐私密码 / 登出 / 断开备份目标。

确认框必须写清：①影响对象与数量 ②是否可恢复 ③恢复路径。**禁止**只写「确定要删除吗？」。

### 6.6 反馈【已定】

| 类型 | 载体 | 用途 |
|---|---|---|
| 轻提示 | 右下角 Toast，自动消失（约 2.8 秒） | 操作已生效 |
| 就地提示 | 表单字段下方 | 校验失败 |
| 面板提示 | 区块内信息条（危险 / 信息两态） | 需要持续可见的说明与后果 |
| 状态栏 | 正文底部状态栏 | 保存与同步状态、大小、加密剩余时间 |

规则：轻提示**不承载需要用户行动的信息**（会被错过）；同一操作不叠加多个轻提示；破坏性操作成功后用警告态，普通成功用成功态。

### 6.7 侧滑详情【已定】

侧滑详情（属性面板）**能力保留，但不由条目列表触发**。计划用途是「主操作区正在使用时临时查看属性」，未来可接的是表格图册的行详情与待办看板的卡片。

【禁止】把它重新接回列表的条目点击——条目点击一律「在右列直接打开」。

### 6.8 动效【已定】

| 场景 | 时长 |
|---|---|
| 悬停 / 小状态变化 | 130–180ms |
| 浮层出现（菜单 / 弹窗） | 160–200ms |
| 侧滑详情 | 260ms |
| 轻提示淡出 | 300ms |

规则：动效只用于**解释空间关系**（从哪来、到哪去），不用于装饰；不使用弹跳、旋转等注意力抢占用动效；全局支持 `prefers-reduced-motion`。

---

## 七、禁止项清单（汇总）【已定】

| # | 禁止项 |
|---|---|
| 1 | 引入 UI 组件库；另立第二套视觉规范 |
| 2 | 组件内硬编码颜色（含 `linear-gradient` 内的色值）、硬编码阴影 / 遮罩 |
| 3 | 组件内硬编码间距值（越过 4.1 的刻度） |
| 4 | 用颜色单独表达状态，不配文字或图标 |
| 5 | 为单个功能另做页面骨架，或绕过「顶栏 + 左右两栏」 |
| 6 | 同一区域放两个并列的实心主色按钮 |
| 7 | 用原生浏览器弹窗（`alert` / `confirm` / `prompt`） |
| 8 | 把警告、破坏性后果、错误校验、实时计数收进 `InfoHint` |
| 9 | 使用「已加密存储」等与「服务端明文 + 前端门禁」模型不符的文案 |
| 10 | 用 emoji 作图标或状态标记；使用字体图标或外部图标库 |
| 11 | 用 `outline:none` 抹掉焦点；让可点击元素不可键盘触达 |
| 12 | 切换主题时整页重渲染 |
| 13 | 页面 `body` 滚动；嵌套双滚动容器 |
| 14 | 新增自定义间距值而不走刻度 |
| 15 | 把侧滑详情重新接回列表条目点击 |
| 16 | 在窄屏布局定型前自行发明移动端布局 |

---

## 八、维护规则【已定】

1. **改动触发**：新增颜色 / 字号 / 间距体系，新增控件形态，修改布局骨架或尺寸，新增禁止项 → 必须同步本文。
2. **与 `components.md` 的分工**：本文写「长什么样、怎么用」，`components.md` 写「有哪些、放哪、什么契约」。同一事实只写一处。
3. **视觉定型时**：补第三章（配色 / 字族 / 字号刻度 / 圆角刻度 / 阴影），并**同批处理** 3.4 的四个问题（收敛字号、收敛圆角、改回归脚本的色板断言、扫描渐变内色值）。
4. **数值变更要重测**：任何标【实测】的值改动后，须跑一遍 `prototype/verify-prototype.js` 与 `prototype/verify-framework.js`，并重新测量相关基线。
5. **本文不写组件 props**：props 属 `components.md`，避免两处漂移。
6. **引用原型不写行号**，只写元素名或选择器（沿用功能拆解 v2.1 约定）。
